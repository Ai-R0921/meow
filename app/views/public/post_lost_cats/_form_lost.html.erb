<% if user_signed_in? %>
  <%= form_with model: [post_lost_cat, post_lost_cat_comment], data: {remote: true} do |f| %>
    <%= f.text_area :comment, placeholder:"comments", class:"comment-box" %>
    <%= f.submit "Post", class:"comment-button" %>
  <% end %>
<% end %>