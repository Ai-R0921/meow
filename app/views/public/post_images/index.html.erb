<h1>Cats</h1>
<% @post_images.each do |post_image| %>
  <%= post_image.title %>
  <%= link_to post_image_path(post_image) do %>
    <% if post_image.image.attached? %>
      <%= image_tag post_image.image, :size => '200x200' %>
    <% end %>
  <% end %>
  <%= post_image.user.user_name %>
  <%= link_to "#{post_image.post_image_comments.count} コメント", post_image_path(post_image) %>
<% end %>

<h1>Ranking</h1>
<% @all_ranks.each do |post_image| %>
  <%= post_image.title %>
  <%= link_to post_image_path(post_image) do %>
    <% if post_image.image.attached? %>
      <%= image_tag post_image.image, :size => '200x200' %>
    <% end %>
  <% end %>
  <%= post_image.user.user_name %>
<% end %>